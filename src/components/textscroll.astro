<div class="textscroll">
  <span>MULTIMEDIEDESIGN, FRONTEND MULTIMEDIEDESIGN, FRONTEND MULTIMEDIEDESIGN, FRONTEND</span>
</div>

<style>
  .textscroll {
    display: flex;
    min-height: 150px;
    width: 100%;
    overflow: hidden;
    white-space: nowrap;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .textscroll span {
    display: inline-block;
    font-family: "polymath", serif;
    font-size: 20em;
    font-weight: 900;
    color: var(--color-black);
    letter-spacing: 0em;
    animation: scroll-text 30s linear infinite;
    will-change: transform;
    transition: color 0.3s;
  }
  html.dark-theme .textscroll span {
    color: var(--color-lightest);
  }
  html:not(.dark-theme) .textscroll span {
    color: var(--color-black);
  }

  @keyframes scroll-text {
    0% {
      transform: translateX(100vw);
    }
    100% {
      transform: translateX(-100vw);
    }
  }
</style>
