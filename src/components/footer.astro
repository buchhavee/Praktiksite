<footer class="footer fade-in-section">
  <div class="footer__background-fade"></div>
  <div class="footer__container">
    <div class="footer__content">
      <h2 class="footer__title">Kontakt</h2>
      <p class="footer__subtitle">Lad os tage en snak :)</p>
      <p class="footer__description">Hvis i har plads til en kreativ og engageret praktikant, så vil jeg meget gerne høre fra jer</p>

      <div class="footer__contact">
        <div class="footer__contact-item">
          <span class="footer__contact-label">Telefon</span>
          <a href="tel:+4524264348" class="footer__contact-link">
            <span class="footer__contact-icon">📞</span>
            +45 24 26 43 48
          </a>
        </div>
        <div class="footer__contact-item">
          <span class="footer__contact-label">Email</span>
          <a href="mailto:buchhavee@gmail.com" class="footer__contact-link">
            <span class="footer__contact-icon">✉️</span>
            buchhavee@gmail.com
          </a>
        </div>
        <div class="footer__contact-item">
          <span class="footer__contact-label">LinkedIn</span>
          <a href="https://www.linkedin.com/in/mads-christiansen-7a5b1a2a0/" target="_blank" rel="noopener noreferrer" class="footer__contact-link">
            <span class="footer__contact-icon">🔗</span>
            Mads Christiansen
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: transparent;
    color: var(--color-text);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    margin-top: 6rem;
    transition: all 0.3s;
  }

  .footer__background-fade {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: var(--color-accent);
    opacity: 0;
    transition: opacity 1.2s ease-in-out;
    z-index: 0;
  }

  .footer.visible .footer__background-fade {
    opacity: 1;
  }

  .footer__container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 4rem;
    position: relative;
    z-index: 1;
  }

  .footer__content {
    text-align: center;
    max-width: 900px;
    margin: 0 auto;
  }

  .footer__title {
    font-size: clamp(2.5rem, 6vw, 5rem);
    font-family: "polymath-display", sans-serif;
    font-weight: 1000;
    margin-bottom: 1rem;
    color: var(--color-text);
    transition: color 0.3s;
    letter-spacing: -0.02em;
  }

  .footer__subtitle {
    font-size: clamp(1.4rem, 3vw, 2.2rem);
    font-weight: 300;
    margin-bottom: 2.5rem;
    color: var(--color-text);
    transition: color 0.3s;
    opacity: 0.9;
  }

  .footer__description {
    font-size: clamp(1.1rem, 2.2vw, 1.6rem);
    font-weight: 200;
    line-height: 1.7;
    margin-bottom: 4rem;
    color: var(--color-text);
    transition: color 0.3s;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    margin-bottom: 4rem;
  }

  .footer__contact {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    align-items: center;
    max-width: 500px;
    margin: 0 auto;
  }

  .footer__contact-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    padding: 1.5rem 2rem;
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    transition: all 0.3s ease;
    width: 100%;
    max-width: 400px;
  }

  .footer__contact-item:hover {
    transform: translateY(-5px);
    background: rgba(255, 255, 255, 0.15);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .footer__contact-label {
    font-size: clamp(0.8rem, 1.5vw, 1rem);
    font-weight: 500;
    color: var(--color-text);
    opacity: 0.7;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    transition: color 0.3s;
  }

  .footer__contact-link {
    color: var(--color-text);
    text-decoration: none;
    font-weight: 500;
    font-size: clamp(1.1rem, 2vw, 1.4rem);
    transition:
      opacity 0.3s ease,
      transform 0.2s ease,
      color 0.3s;
    display: flex;
    align-items: center;
    gap: 0.8rem;
  }

  .footer__contact-icon {
    font-size: 1.2em;
    transition: transform 0.3s ease;
  }

  .footer__contact-link:hover .footer__contact-icon {
    transform: scale(1.2) rotate(10deg);
  }

  /* Når baggrunden fader ind, ændres tekstfarven til at kontrastere */
  .footer.visible .footer__title,
  .footer.visible .footer__subtitle,
  .footer.visible .footer__description,
  .footer.visible .footer__contact-link,
  .footer.visible .footer__contact-label {
    color: var(--color-darkest);
  }

  .footer.visible .footer__contact-item {
    background: rgba(0, 0, 0, 0.1);
    border-color: rgba(0, 0, 0, 0.2);
  }

  .footer.visible .footer__contact-item:hover {
    background: rgba(0, 0, 0, 0.15);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
  }

  /* Dark theme adjustments */
  html.dark-theme .footer.visible .footer__title,
  html.dark-theme .footer.visible .footer__subtitle,
  html.dark-theme .footer.visible .footer__description,
  html.dark-theme .footer.visible .footer__contact-link,
  html.dark-theme .footer.visible .footer__contact-label {
    color: var(--color-lightest);
  }

  html.dark-theme .footer.visible .footer__contact-item {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.2);
  }

  html.dark-theme .footer.visible .footer__contact-item:hover {
    background: rgba(255, 255, 255, 0.15);
  }

  /* Mobile responsive - følger samme padding som resten af siden */
  @media (max-width: 1199px) and (min-width: 768px) {
    .footer__container {
      padding: 0 2rem;
    }

    .footer__contact-item {
      padding: 1.2rem 1.5rem;
    }
  }

  @media (max-width: 767px) and (min-width: 451px) {
    .footer__container {
      padding: 0 1rem;
    }

    .footer {
      padding: 2rem 0;
      min-height: 80vh;
    }

    .footer__contact {
      gap: 1.5rem;
    }

    .footer__contact-item {
      padding: 1rem 1.2rem;
    }
  }

  @media (max-width: 450px) {
    .footer__container {
      padding: 0 1rem;
    }

    .footer {
      padding: 2rem 0;
      min-height: 70vh;
    }

    .footer__contact {
      gap: 1rem;
    }

    .footer__contact-item {
      padding: 0.8rem 1rem;
    }

    .footer__contact-link {
      flex-direction: column;
      gap: 0.5rem;
    }
  }
</style>
