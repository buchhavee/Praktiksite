<footer id="footer" class="footer">
  <div class="footer__container">
    <div class="footer__header">
      <div class="footer__info-row">
        <div class="footer__left-info">
          <span class="footer__tagline">Multimediedesign, EK</span>
        </div>
        <div class="footer__availability">
          <span class="footer__status">Praktikforløb</span>
          <span class="footer__date">d. 19. januar <br />til 26. marts 2026</span>
        </div>
      </div>
    </div>

    <div class="footer__main">
      <h2 class="footer__title">INTERESSERET I AT<br />ARBEJDE SAMMEN?</h2>
      <div class="footer__hand-emoji">✌️</div>
    </div>

    <div class="footer__contact">
      <p class="footer__contact-text">Send mig en mail eller ring på:</p>
      <a href="mailto:buchhavee@gmail.com" class="footer__email">buchhavee@gmail.com</a>
      <a href="tel:+4524264348" class="footer__email">+45 24 26 43 48</a>
    </div>

    <div class="footer__bottom">
      <div class="footer__social">
        <a href="https://www.linkedin.com/in/mads-christiansen-02a951105/" target="_blank" class="footer__social-link">LINKEDIN</a>
        <a href="mailto:buchhavee@gmail.com" class="footer__social-link">EMAIL</a>
        <a href="https://www.instagram.com/buchhavee/" target="_blank" class="footer__social-link">INSTAGRAM</a>
        <a href="https://github.com/buchhavee" target="_blank" class="footer__social-link">GITHUB</a>
      </div>

      <div class="footer__final-row">
        <div class="footer__credits"></div>

        <div class="footer__back-to-top">
          <a href="#" class="footer__top-link">🔝</a>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-medium);
    color: var(--color-lightest);
    padding: 2rem;
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    transition:
      background 0.3s,
      color 0.3s;
  }

  html.dark-theme .footer {
    background: var(--color-darkest);
  }

  .footer__container {
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    min-height: 80vh;
  }

  .footer__header {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    font-size: 0.9rem;
    line-height: 1.4;
    margin-bottom: 2rem;
  }

  .footer__info-row {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
  }

  .footer__left-info {
    display: flex;
  }

  .footer__tagline {
    opacity: 0.8;
    font-size: 0.85rem;
    color: var(--color-text);
    transition: color 0.3s;
  }

  .footer__availability {
    display: flex;
    flex-direction: column;
    text-align: right;
    gap: 0.2rem;
  }

  .footer__status,
  .footer__date {
    opacity: 0.8;
    font-size: 0.85rem;
    color: var(--color-text);
    transition: color 0.3s;
  }

  .footer__main {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    position: relative;
    flex: 1;
    margin-bottom: 2rem;
  }

  .footer__title {
    font-family: "polymath-display", sans-serif;
    font-weight: 900;
    font-size: clamp(3rem, 8vw, 8rem);
    line-height: 0.9;
    letter-spacing: -0.02em;
    margin: 0;
    position: relative;
    color: var(--color-text);
    transition: color 0.3s;
  }

  .footer__hand-emoji {
    position: absolute;
    font-size: clamp(2rem, 4vw, 4rem);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
  }

  .footer__contact {
    display: flex;
    flex-direction: column;
    text-align: center;
    margin-bottom: 2rem;
    gap: 1rem;
  }

  .footer__contact-text {
    font-size: 1.1rem;
    margin-bottom: 1rem;
    opacity: 0.9;
    color: var(--color-text);
    transition: color 0.3s;
  }

  .footer__email {
    color: var(--color-text);
    text-decoration: none;
    font-size: clamp(1.2rem, 3vw, 2rem);
    font-weight: 500;
    transition: all 0.3s ease;
  }

  .footer__email:hover {
    opacity: 0.7;
    transform: scale(1.05);
    transition: all 0.3s ease;
  }

  .footer__bottom {
    display: flex;
    flex-direction: column;
    font-size: 0.85rem;
  }

  .footer__social {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    justify-content: center;
    margin-bottom: 1rem;
  }

  .footer__social-link {
    color: var(--color-text);
    text-decoration: none;
    font-size: 0.8rem;
    padding: 0.5rem 1rem;
    border: 1px solid var(--color-dark);
    border-radius: 20px;
    transition: all 0.3s ease;
  }

  .footer__social-link:hover {
    background: var(--color-accent);
    border-color: var(--color-dark);
  }

  .footer__final-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer__credits p {
    margin: 0 0 0.5rem 0;
    color: var(--color-text);
    transition: color 0.3s;
  }

  .footer__credit-link {
    color: var(--color-text);
    text-decoration: underline;
    opacity: 0.8;
    transition: color 0.3s;
  }

  .footer__back-to-top {
    text-align: right;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
  }

  .footer__top-link {
    color: var(--color-text);
    text-decoration: none;
    opacity: 0.8;
    line-height: 1.4;
    text-align: right;
    transition: color 0.3s;
  }

  @media (max-width: 768px) {
    .footer {
      padding: 3rem 1rem 0 1rem;
    }

    .footer__title {
      font-size: clamp(2.5rem, 12vw, 4rem);
    }

    .footer__hand-emoji {
      font-size: clamp(2rem, 6vw, 4rem);
    }
  }
</style>
