<section class="section-split fade-in-section" id="about">
  <div class="section-split__inner">
    <div class="section-divider"></div>
    <div class="section-content">
      <div class="section-split__header fade-in-left">
        <h2>OM MIG</h2>
      </div>
      <div class="section-split__content">
        <p>Jeg har altid haft en stor passion for at udtrykke mig kreativt, gennem musik og design. <br /> <br /> Derudover så elsker jeg at lære nye færdigheder og udfordre mig selv. <br /> <br />Som musikproducer er jeg vant til at jonglere flere projekter på én gang. Den erfaring tager jeg med mig som multimediedesign-studerende på Erhvervsakademiet København, hvor jeg har valgt at fokusere på frontend.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .section-split__inner {
    width: 100%;
    box-sizing: border-box;
  }

  .section-divider {
    position: absolute;
    left: 4rem;
    right: 4rem;
    height: 2px;
    background: var(--color-dark);
    content: "";
    z-index: 1;
    opacity: 1;
    pointer-events: none;
    display: block;
  }

  .section-content {
    display: flex;
    flex-direction: row;
    position: relative;
    padding-block: 2rem;
    gap: 1rem;
  }

  .section-split__header {
    flex: 1 1;
    display: flex;
    align-items: flex-start;
    justify-content: flex-start;
  }
  .section-split__header h2 {
    font-size: 4rem;
    font-family: "polymath-display", sans-serif;
    font-weight: 1000;
    margin: 0;
  }
  .section-split__content {
    flex: 1 1 0;
    display: flex;
    align-items: center;
    justify-content: flex-end;
  }
  .section-split__content p {
    font-weight: 100;
    margin: 0;
  }
</style>
